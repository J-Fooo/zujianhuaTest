<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Order_MainActivity"
    android:padding="5dp"
    >

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:id="@+id/tv_title"
        android:text="Order"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_ck_per1"
        style="@style/OrderTextViewStyle"
        app:layout_constraintTop_toBottomOf="@id/tv_title"
        app:layout_constraintLeft_toLeftOf="@id/tv_title"
        android:text="to personal 1"
        android:onClick="toMain"
        />

    <TextView
        style="@style/OrderTextViewStyle"
        app:layout_constraintTop_toBottomOf="@id/tv_ck_per1"
        app:layout_constraintLeft_toLeftOf="@id/tv_ck_per1"
        android:text="to personal 2"
        android:onClick="toPersonal"
        />
</androidx.constraintlayout.widget.ConstraintLayout>